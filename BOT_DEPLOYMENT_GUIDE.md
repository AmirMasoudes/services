# راهنمای دیپلوی ربات‌های تلگرام 🤖

## مراحل دیپلوی ربات‌ها

### 1️⃣ آماده‌سازی توکن‌ها

ابتدا باید توکن‌های ربات‌های تلگرام را از BotFather دریافت کنید:

#### برای ربات کاربر:
1. به @BotFather در تلگرام پیام دهید
2. دستور `/newbot` را بزنید
3. نام ربات را وارد کنید (مثل: VPN User Bot)
4. نام کاربری ربات را وارد کنید (مثل: vpn_user_bot)
5. توکن دریافتی را کپی کنید

#### برای ربات ادمین:
1. دوباره به @BotFather پیام دهید
2. دستور `/newbot` را بزنید
3. نام ربات را وارد کنید (مثل: VPN Admin Bot)
4. نام کاربری ربات را وارد کنید (مثل: vpn_admin_bot)
5. توکن دریافتی را کپی کنید

### 2️⃣ تنظیم فایل .env

فایل `.env` را در مسیر اصلی پروژه ایجاد کنید:

```bash
# ایجاد فایل .env
cp env_example.txt .env
```

سپس فایل `.env` را ویرایش کنید:

```env
# Django Settings
SECRET_KEY=django-insecure-c^1%va7g4+yqfygvbjku#d4-4d8-sw8rzw9!$_wq-vt(*x-mw9
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Telegram Bot Tokens
USER_BOT_TOKEN=your_user_bot_token_here
ADMIN_BOT_TOKEN=your_admin_bot_token_here

# Admin Password
ADMIN_PASSWORD=admin123

# X-UI Server Settings
XUI_SERVER_HOST=localhost
XUI_SERVER_PORT=54321
XUI_USERNAME=admin
XUI_PASSWORD=admin
```

### 3️⃣ نصب وابستگی‌ها

```bash
# نصب پکیج‌های مورد نیاز
pip install python-telegram-bot django python-dotenv psutil
```

### 4️⃣ راه‌اندازی ربات‌ها

#### روش 1: راه‌اندازی ساده
```bash
# راه‌اندازی هر دو ربات
python start_bots_simple.py
```

#### روش 2: راه‌اندازی جداگانه
```bash
# راه‌اندازی ربات کاربر
python bot/user_bot.py &

# راه‌اندازی ربات ادمین
python bot/admin_boy.py &
```

#### روش 3: استفاده از systemd (پیشنهادی)
```bash
# اجرای اسکریپت دیپلوی
python deploy_bots.py
```

### 5️⃣ بررسی وضعیت ربات‌ها

#### بررسی سرویس‌ها:
```bash
# وضعیت سرویس ربات کاربر
systemctl status vpn-user-bot.service

# وضعیت سرویس ربات ادمین
systemctl status vpn-admin-bot.service
```

#### مشاهده لاگ‌ها:
```bash
# لاگ‌های ربات کاربر
journalctl -u vpn-user-bot.service -f

# لاگ‌های ربات ادمین
journalctl -u vpn-admin-bot.service -f
```

### 6️⃣ تست ربات‌ها

#### تست ربات کاربر:
1. ربات کاربر را در تلگرام پیدا کنید
2. دستور `/start` را بزنید
3. باید پیام خوش‌آمدگویی دریافت کنید

#### تست ربات ادمین:
1. ربات ادمین را در تلگرام پیدا کنید
2. دستور `/start` را بزنید
3. رمز عبور ادمین را وارد کنید (پیش‌فرض: admin123)
4. باید پنل ادمین نمایش داده شود

### 7️⃣ دستورات مفید

#### راه‌اندازی:
```bash
# راه‌اندازی سرویس‌ها
systemctl start vpn-user-bot.service
systemctl start vpn-admin-bot.service

# راه‌اندازی خودکار در بوت
systemctl enable vpn-user-bot.service
systemctl enable vpn-admin-bot.service
```

#### توقف:
```bash
# توقف سرویس‌ها
systemctl stop vpn-user-bot.service
systemctl stop vpn-admin-bot.service

# یا استفاده از اسکریپت
./stop_bots.sh
```

#### راه‌اندازی مجدد:
```bash
# راه‌اندازی مجدد سرویس‌ها
systemctl restart vpn-user-bot.service
systemctl restart vpn-admin-bot.service
```

### 8️⃣ عیب‌یابی

#### مشکلات رایج:

**1. خطای توکن:**
```
❌ USER_BOT_TOKEN تعریف نشده است
```
**راه حل:** فایل `.env` را بررسی کنید و توکن‌ها را درست وارد کنید.

**2. خطای اتصال:**
```
❌ خطا در اتصال به تلگرام
```
**راه حل:** 
- اینترنت سرور را بررسی کنید
- توکن‌ها را دوباره بررسی کنید
- فایروال را بررسی کنید

**3. خطای Django:**
```
❌ خطا در تنظیمات Django
```
**راه حل:**
```bash
# اجرای migrations
python manage.py makemigrations
python manage.py migrate

# ایجاد superuser
python manage.py createsuperuser
```

**4. خطای وابستگی‌ها:**
```
❌ ModuleNotFoundError
```
**راه حل:**
```bash
# نصب وابستگی‌ها
pip install -r requirements.txt
```

### 9️⃣ تنظیمات پیشرفته

#### تنظیم Webhook (اختیاری):
```env
# در فایل .env
BOT_WEBHOOK_URL=https://your-domain.com/webhook
BOT_WEBHOOK_PATH=/webhook
```

#### تنظیم لاگینگ:
```env
# در فایل .env
LOG_LEVEL=DEBUG
LOG_FILE=/var/log/vpn-bots.log
```

#### تنظیم امنیتی:
```env
# در فایل .env
ALLOWED_USERS=123456789,987654321
ADMIN_USERS=123456789
```

### 🔟 نکات مهم

1. **امنیت:** رمز عبور ادمین را تغییر دهید
2. **بکاپ:** فایل `.env` را بکاپ بگیرید
3. **مانیتورینگ:** لاگ‌ها را مرتب بررسی کنید
4. **به‌روزرسانی:** ربات‌ها را مرتب به‌روزرسانی کنید
5. **محدودیت‌ها:** تعداد کاربران همزمان را محدود کنید

### 📞 پشتیبانی

در صورت مشکل:
1. لاگ‌ها را بررسی کنید
2. وضعیت سرویس‌ها را چک کنید
3. توکن‌ها را دوباره بررسی کنید
4. وابستگی‌ها را نصب کنید

---

**🎉 تبریک! ربات‌های شما آماده استفاده هستند!** 
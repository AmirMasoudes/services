# 🚀 راهنمای نهایی راه‌اندازی سیستم ادمین X-UI

## ✅ وضعیت فعلی

- ✅ مدل‌های جدید ایجاد شدند
- ✅ پنل ادمین Django آماده است
- ✅ ربات ادمین آماده است
- ✅ سرویس‌ها و API ها کار می‌کنند

## 🔧 مراحل نهایی

### 1. تست اتصال ربات

```bash
python test_bot_connection.py
```

### 2. راه‌اندازی سرویس

```bash
# کپی فایل سرویس
cp admin-bot.service /etc/systemd/system/

# بارگذاری مجدد systemd
systemctl daemon-reload

# فعال‌سازی سرویس
systemctl enable admin-bot

# شروع سرویس
systemctl start admin-bot

# بررسی وضعیت
systemctl status admin-bot
```

### 3. بررسی لاگ‌ها

```bash
# مشاهده لاگ‌های سرویس
journalctl -u admin-bot -f

# مشاهده لاگ‌های ربات
tail -f /opt/vpn-service/services/bot.log
```

## 🎯 ویژگی‌های آماده

### پنل ادمین Django

- **مدیریت سرورها**: اضافه/ویرایش/حذف سرورهای X-UI
- **مدیریت Inbound ها**: کنترل پورت‌ها، پروتکل‌ها، ظرفیت
- **مدیریت کلاینت‌ها**: نظارت بر ترافیک، انقضا، وضعیت
- **تخصیص هوشمند**: انتخاب خودکار Inbound مناسب
- **همگام‌سازی**: به‌روزرسانی خودکار با X-UI

### ربات تلگرام ادمین

- **احراز هویت**: رمز عبور + بررسی ID
- **داشبورد**: آمار کلی سیستم
- **مدیریت کاربران**: مشاهده، اضافه، حذف
- **مدیریت Inbound ها**: مشاهده، اضافه، ویرایش
- **مدیریت کلاینت‌ها**: نظارت، کنترل ترافیک
- **دستورات سریع**: عملیات رایج

## 🔐 امنیت

### پنل ادمین

- دسترسی محدود به ادمین‌ها
- لاگ تمام عملیات
- بررسی مجوزها

### ربات تلگرام

- رمز عبور اجباری
- بررسی ID تلگرام
- محدودیت دسترسی

## 📊 مانیتورینگ

### آمار کلی

- تعداد کاربران فعال
- تعداد Inbound های فعال
- ترافیک کل سیستم
- وضعیت سرورها

### هشدارها

- Inbound پر ظرفیت
- کلاینت‌های منقضی
- خطاهای اتصال
- ترافیک بالا

## 🛠️ عیب‌یابی

### مشکل اتصال ربات

```bash
# تست اتصال
python test_bot_connection.py

# بررسی فایروال
iptables -L

# بررسی DNS
nslookup api.telegram.org
```

### مشکل پنل ادمین

```bash
# بررسی لاگ‌ها
tail -f /var/log/django.log

# تست مدل‌ها
python manage.py shell
```

### مشکل سرویس

```bash
# بررسی وضعیت
systemctl status admin-bot

# راه‌اندازی مجدد
systemctl restart admin-bot

# مشاهده لاگ‌ها
journalctl -u admin-bot -f
```

## 📋 دستورات مفید

### مدیریت سرویس

```bash
# شروع
systemctl start admin-bot

# توقف
systemctl stop admin-bot

# راه‌اندازی مجدد
systemctl restart admin-bot

# وضعیت
systemctl status admin-bot

# فعال‌سازی
systemctl enable admin-bot
```

### مدیریت Django

```bash
# اجرای سرور
python manage.py runserver 0.0.0.0:8000

# ایجاد superuser
python manage.py createsuperuser

# بررسی مدل‌ها
python manage.py shell
```

### تست سیستم

```bash
# تست کامل
python test_admin_system.py

# تست اتصال
python test_bot_connection.py

# تست API
python test_enhanced_api_models.py
```

## 🎉 موفقیت!

سیستم ادمین X-UI شما آماده استفاده است!

### دسترسی‌ها:

- **پنل ادمین**: http://your-server:8000/admin/
- **ربات تلگرام**: @your_bot_username
- **API**: http://your-server:8000/api/

### پشتیبانی:

- لاگ‌ها در `/var/log/`
- تنظیمات در `config/settings.py`
- مستندات در فایل‌های `.md`

---

**توسعه‌دهنده**: سیستم ادمین X-UI  
**نسخه**: 1.0  
**تاریخ**: 2025-08-03

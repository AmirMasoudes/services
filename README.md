# 🤖 ربات تلگرام VPN با اتصال X-UI

یک سیستم کامل ربات تلگرام برای فروش و مدیریت کانفیگ‌های VPN با اتصال مستقیم به X-UI.

## ✨ ویژگی‌ها

### 🔧 **سیستم اصلی**
- ✅ **ثبت‌نام خودکار** با اطلاعات تلگرام
- ✅ **پلن تستی رایگان** (24 ساعت)
- ✅ **سیستم خرید پلن** با پرداخت
- ✅ **مدیریت کانفیگ‌ها** در X-UI
- ✅ **ربات ادمین** برای مدیریت کامل

### 🎁 **پلن تستی**
- هر کاربر فقط **یک بار** می‌تواند استفاده کند
- **24 ساعت اعتبار** رایگان
- **کانفیگ واقعی** در X-UI
- **حجم نامحدود** برای تست

### 🖥️ **اتصال X-UI**
- **اتصال مستقیم** به سرورهای X-UI
- **ایجاد خودکار کاربر** در X-UI
- **تولید کانفیگ واقعی** (VMess/VLess)
- **مدیریت ترافیک** و انقضا
- **پشتیبانی از چندین سرور**

## 🚀 نصب و راه‌اندازی

### 1. پیش‌نیازها
```bash
# نصب Python 3.8+
# نصب X-UI روی سرور
# نصب Git
```

### 2. کلون کردن پروژه
```bash
git clone https://github.com/your-repo/configvpn.git
cd configvpn
```

### 3. نصب وابستگی‌ها
```bash
# ایجاد محیط مجازی
python -m venv venv

# فعال‌سازی محیط مجازی
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# نصب پکیج‌ها
pip install -r requirements.txt
```

### 4. تنظیم متغیرهای محیطی
فایل `.env` را ایجاد کنید:
```env
# توکن‌های ربات
TELEGRAM_BOT_TOKEN=your_user_bot_token
ADMIN_BOT_TOKEN=your_admin_bot_token

# رمز عبور ادمین
ADMIN_PASSWORD=your_admin_password

# تنظیمات سرور X-UI (اختیاری)
XUI_SERVER_HOST=your-server-ip.com
XUI_SERVER_PORT=54321
XUI_USERNAME=admin
XUI_PASSWORD=your-xui-password
```

### 5. راه‌اندازی دیتابیس
```bash
# ایجاد migration ها
python manage.py makemigrations

# اعمال migration ها
python manage.py migrate

# راه‌اندازی اولیه
python setup_bot.py
```

### 6. راه‌اندازی X-UI
```bash
# راه‌اندازی سرور X-UI
python setup_xui_server.py

# برای راهنمای کامل:
python setup_xui_server.py help
```

### 7. اجرای ربات‌ها
```bash
# ربات کاربر (در ترمینال جداگانه)
python bot/user_bot.py

# ربات ادمین (در ترمینال جداگانه)
python bot/admin_boy.py
```

## 🖥️ راه‌اندازی X-UI

### 1. نصب X-UI
```bash
# نصب X-UI
bash <(curl -Ls https://raw.githubusercontent.com/vaxilu/x-ui/master/install.sh)
```

### 2. تنظیم X-UI
1. به پنل X-UI وارد شوید: `http://your-server-ip:54321`
2. نام کاربری و رمز عبور تنظیم کنید
3. یک **Inbound** ایجاد کنید:
   - **Protocol**: VMess یا VLess
   - **Port**: 443 (برای HTTPS)
   - **Security**: TLS
   - **Path**: / (یا مسیر دلخواه)

### 3. اتصال به ربات
```bash
# اجرای اسکریپت راه‌اندازی
python setup_xui_server.py

# وارد کردن اطلاعات سرور:
# نام,آدرس,پورت,نام‌کاربری,رمزعبور
# مثال: سرور اصلی,192.168.1.100,54321,admin,password123
```

## 📋 ساختار پروژه

```
configvpn/
├── accounts/           # مدل‌های کاربران
├── bot/               # ربات‌های تلگرام
│   ├── user_bot.py    # ربات کاربر
│   └── admin_boy.py   # ربات ادمین
├── conf/              # تنظیمات و کانفیگ‌ها
├── order/             # سفارشات و پرداخت‌ها
├── plan/              # پلن‌ها
├── xui_servers/       # مدیریت سرورهای X-UI
│   ├── models.py      # مدل‌های سرور
│   └── services.py    # سرویس‌های X-UI
├── core/              # مدل‌های پایه
├── config/            # تنظیمات جنگو
├── setup_bot.py       # راه‌اندازی اولیه
├── setup_xui_server.py # راه‌اندازی X-UI
└── requirements.txt   # وابستگی‌ها
```

## 🔧 ویژگی‌های ربات کاربر

### دستورات اصلی
- `/start` - شروع و ثبت‌نام
- `/profile` - نمایش پروفایل
- `/my_plans` - پلن‌های من
- `/my_config` - تنظیمات من

### منوهای اصلی
- 🛒 **خرید پلن** - انتخاب و خرید پلن
- 📊 **پروفایل من** - اطلاعات کاربر
- 📦 **پلن‌های من** - کانفیگ‌های فعال
- ⚙️ **تنظیمات من** - کانفیگ‌های VPN
- 🎁 **پلن تستی** - دریافت پلن رایگان

## 🔧 ویژگی‌های ربات ادمین

### منوهای مدیریت
- 👥 **لیست کاربران** - مشاهده کاربران
- 📦 **لیست پلن‌ها** - مدیریت پلن‌ها
- ➕ **افزودن پلن** - ایجاد پلن جدید
- 💰 **درخواست‌های پرداخت** - تایید/رد پرداخت
- 📨 **ارسال پیام** - ارسال پیام به کاربران
- 📊 **آمار کلی** - آمار سیستم
- 🎁 **مدیریت پلن‌های تستی** - مشاهده کانفیگ‌های تستی
- 🖥️ **مدیریت سرورهای X-UI** - مدیریت سرورها

## 🎁 سیستم پلن تستی

### ویژگی‌ها
- ✅ **یک بار استفاده** برای هر کاربر
- ⏰ **24 ساعت اعتبار**
- 📊 **حجم نامحدود**
- 🔧 **کانفیگ واقعی** در X-UI
- 🖥️ **اتصال مستقیم** به سرور

### نحوه استفاده
1. کاربر `/start` می‌زند
2. 🎁 **پلن تستی** را انتخاب می‌کند
3. سیستم بررسی می‌کند:
   - آیا قبلاً استفاده کرده؟
   - اگر نه، کانفیگ در X-UI ایجاد می‌شود
4. کانفیگ تستی نمایش داده می‌شود
5. کاربر می‌تواند 24 ساعت استفاده کند

## 🖥️ اتصال X-UI

### سرویس‌های موجود
- **XUIService** - اتصال به X-UI
- **ConfigGenerator** - تولید کانفیگ
- **UserConfigService** - مدیریت کانفیگ کاربران

### API های پشتیبانی شده
- ✅ ورود به X-UI
- ✅ دریافت لیست inbound ها
- ✅ ایجاد کاربر در X-UI
- ✅ حذف کاربر از X-UI
- ✅ به‌روزرسانی ترافیک

### پروتکل‌های پشتیبانی شده
- ✅ **VMess** - با WebSocket و TLS
- ✅ **VLess** - با WebSocket و TLS
- ✅ **Trojan** - با TLS

## 📊 آمار و گزارش‌گیری

### آمار کاربران
- تعداد کل کاربران
- کاربران فعال
- استفاده از پلن تستی
- کانفیگ‌های فعال

### آمار سرورها
- تعداد سرورهای X-UI
- سرورهای فعال
- تعداد inbound ها
- وضعیت اتصال

### آمار مالی
- تعداد سفارشات
- درآمد کل
- درخواست‌های در انتظار
- پلن‌های پرفروش

## 🔒 امنیت

### ویژگی‌های امنیتی
- ✅ **رمزنگاری** کانفیگ‌ها
- ✅ **احراز هویت** ادمین
- ✅ **محدودیت استفاده** پلن تستی
- ✅ **مدیریت ترافیک** کاربران
- ✅ **انقضای خودکار** کانفیگ‌ها

### بهترین شیوه‌ها
- 🔐 استفاده از HTTPS
- 🔐 رمزهای قوی
- 🔐 به‌روزرسانی منظم
- 🔐 پشتیبان‌گیری از دیتابیس

## 🛠️ عیب‌یابی

### مشکلات رایج

#### خطا در اتصال به X-UI
```bash
# بررسی تنظیمات سرور
python setup_xui_server.py

# بررسی فایروال
sudo ufw allow 54321
sudo ufw allow 443
```

#### خطا در ایجاد کانفیگ
```bash
# بررسی inbound ها در X-UI
# اطمینان از وجود inbound فعال
# بررسی تنظیمات SSL
```

#### خطا در ربات
```bash
# بررسی توکن‌ها
# بررسی متغیرهای محیطی
# بررسی اتصال اینترنت
```

## 📝 تغییرات اخیر

### نسخه 2.0.0
- ✅ **اتصال کامل به X-UI**
- ✅ **کانفیگ‌های واقعی**
- ✅ **مدیریت چندین سرور**
- ✅ **سیستم پلن تستی بهبود یافته**
- ✅ **آمار و گزارش‌گیری کامل**

### نسخه 1.0.0
- ✅ سیستم پایه ربات
- ✅ مدیریت کاربران
- ✅ سیستم خرید پلن
- ✅ پلن تستی ساده

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Pull Request ارسال کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 📞 پشتیبانی

برای پشتیبانی:
- 📧 ایمیل: support@example.com
- 💬 تلگرام: @support_bot
- 📖 مستندات: [Wiki](https://github.com/your-repo/wiki)

---

**نکته مهم**: حتماً قبل از استفاده در محیط تولید، تمام تنظیمات امنیتی را بررسی کنید.

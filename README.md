# سیستم مدیریت سرویس VPN

این پروژه یک سیستم کامل مدیریت سرویس VPN است که شامل ربات تلگرام، پنل ادمین، و مدیریت سرورهای X-UI می‌باشد.

## 🚀 ویژگی‌ها

- **ربات تلگرام کاربران**: برای خرید پلن، دریافت کانفیگ تستی، و مدیریت حساب کاربری
- **ربات تلگرام ادمین**: برای مدیریت سرورها، کاربران، و سفارشات
- **پنل ادمین Django**: برای مدیریت کامل سیستم
- **مدیریت سرورهای X-UI**: اتصال و مدیریت خودکار سرورهای VPN
- **سیستم پرداخت**: مدیریت سفارشات و پرداخت‌ها
- **کانفیگ تستی**: ارائه کانفیگ رایگان 24 ساعته

## 📁 ساختار پروژه

```
services/
├── accounts/          # مدیریت کاربران
├── bot/              # ربات‌های تلگرام
├── chat_messages/    # پیام‌های چت
├── conf/             # تنظیمات کانفیگ
├── config/           # تنظیمات اصلی Django
├── core/             # مدل‌های پایه
├── order/            # مدیریت سفارشات
├── plan/             # مدیریت پلن‌ها
├── templates/        # قالب‌های HTML
├── xui_servers/      # مدیریت سرورهای X-UI
├── requirements.txt  # وابستگی‌های Python
└── README.md        # این فایل
```

## 🛠️ نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8+
- Django 5.2+
- PostgreSQL (اختیاری، SQLite به صورت پیش‌فرض)

### نصب

1. **کلون کردن پروژه**
```bash
git clone <repository-url>
cd services
```

2. **ایجاد محیط مجازی**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# یا
venv\Scripts\activate     # Windows
```

3. **نصب وابستگی‌ها**
```bash
pip install -r requirements.txt
```

4. **تنظیم متغیرهای محیطی**
```bash
# فایل .env ایجاد کنید
cp .env.example .env
# و مقادیر زیر را تنظیم کنید:
ADMIN_BOT_TOKEN=your_admin_bot_token
ADMIN_PASSWORD=your_admin_password
```

5. **اجرای مایگریشن‌ها**
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **ایجاد سوپر یوزر**
```bash
python manage.py createsuperuser
```

7. **اجرای سرور توسعه**
```bash
python manage.py runserver
```

## 🤖 راه‌اندازی ربات‌ها

### ربات کاربران
```bash
python bot/user_bot.py
```

### ربات ادمین
```bash
python bot/admin_bot.py
```

## 📊 مدل‌های داده

### کاربران (accounts)
- `UsersModel`: مدل اصلی کاربران با اطلاعات تلگرام

### سرورها (xui_servers)
- `XUIServer`: سرورهای X-UI
- `XUIInbound`: Inbound های سرور
- `XUIClient`: کلاینت‌های سرور
- `UserConfig`: کانفیگ‌های کاربران

### سفارشات (order)
- `OrderUserModel`: سفارشات کاربران
- `PayMentModel`: پرداخت‌ها

### پلن‌ها (plan)
- `ConfingPlansModel`: پلن‌های موجود

### تنظیمات (conf)
- `ConfigUserModel`: تنظیمات کاربران
- `TrialConfigModel`: کانفیگ‌های تستی

## 🔧 تنظیمات

### تنظیمات Django (config/settings.py)

```python
# تنظیمات ربات ادمین
ADMIN_BOT_TOKEN = 'your_bot_token'
ADMIN_PASSWORD = 'your_admin_password'
ADMIN_USER_IDS = [123456789]  # ID های ادمین تلگرام
```

### تنظیمات سرورهای X-UI

در پنل ادمین Django می‌توانید سرورهای X-UI را اضافه کنید:
- نام سرور
- آدرس سرور
- پورت
- نام کاربری و رمز عبور

## 📱 API Endpoints

### کاربران
- `GET /api/users/` - لیست کاربران
- `POST /api/users/` - ایجاد کاربر جدید

### سفارشات
- `GET /api/orders/` - لیست سفارشات
- `POST /api/orders/` - ایجاد سفارش جدید

### کانفیگ‌ها
- `GET /api/configs/` - لیست کانفیگ‌ها
- `POST /api/configs/` - ایجاد کانفیگ جدید

## 🔒 امنیت

- تمام API ها نیاز به احراز هویت دارند
- ربات‌ها با توکن‌های جداگانه کار می‌کنند
- رمزهای عبور در فایل‌های محیطی ذخیره می‌شوند
- Soft Delete برای حذف امن داده‌ها

## 🚀 استقرار

### تولید (Production)

1. **تنظیم DEBUG = False**
2. **استفاده از دیتابیس PostgreSQL**
3. **تنظیم ALLOWED_HOSTS**
4. **استفاده از HTTPS**
5. **تنظیم STATIC_ROOT و MEDIA_ROOT**

### Docker (اختیاری)

```dockerfile
FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
```

## 📝 لاگ‌ها

لاگ‌های سیستم در فایل‌های زیر ذخیره می‌شوند:
- `logs/django.log` - لاگ‌های Django
- `logs/bot.log` - لاگ‌های ربات‌ها

## 🤝 مشارکت

برای مشارکت در پروژه:
1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Pull Request ارسال کنید

## 📞 پشتیبانی

برای سوالات و مشکلات:
- Issue در GitHub ایجاد کنید
- با تیم توسعه تماس بگیرید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

**نکته**: این سیستم برای مدیریت سرویس VPN طراحی شده و نیاز به تنظیمات مناسب امنیتی دارد. 
# راهنمای تنظیم فایل .env

## 📋 مقدمه

این راهنما به شما کمک می‌کند تا فایل `.env` را برای پروژه VPN Bot تنظیم کنید. تمام تنظیمات اصلی حساب و پروژه در این فایل قرار می‌گیرند.

## 🚀 مراحل راه‌اندازی

### 1. ایجاد فایل .env

```bash
# کپی کردن فایل نمونه
cp env_config.env .env

# یا ایجاد فایل جدید
touch .env
```

### 2. تنظیمات اجباری

#### 🔑 تنظیمات Django
```bash
# کلید امنیتی Django - حتماً تغییر دهید!
SECRET_KEY=your-very-secure-secret-key-here

# حالت توسعه
DEBUG=True

# میزبان‌های مجاز
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
```

#### 🤖 تنظیمات ربات‌های تلگرام
```bash
# توکن ربات ادمین - از @BotFather دریافت کنید
ADMIN_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# توکن ربات کاربران - از @BotFather دریافت کنید
USER_BOT_TOKEN=1234567890:XYZabcDEFghiJKLmnoPQRstuVWXyz

# رمز عبور ادمین
ADMIN_PASSWORD=your-secure-admin-password

# ID های ادمین تلگرام (با کاما جدا کنید)
# برای پیدا کردن ID خود، به @userinfobot پیام دهید
ADMIN_USER_IDS=123456789,987654321
```

#### 🖥️ تنظیمات سرور X-UI
```bash
# آدرس سرور X-UI
XUI_DEFAULT_HOST=your-xui-server.com

# پورت سرور X-UI
XUI_DEFAULT_PORT=54321

# نام کاربری X-UI
XUI_DEFAULT_USERNAME=admin

# رمز عبور X-UI
XUI_DEFAULT_PASSWORD=your-xui-password

# مسیر پایه وب X-UI (اختیاری)
XUI_WEB_BASE_PATH=/
```

### 3. تنظیمات اختیاری

#### 📧 تنظیمات ایمیل
```bash
# سرور SMTP
EMAIL_HOST=smtp.gmail.com

# پورت SMTP
EMAIL_PORT=587

# استفاده از TLS
EMAIL_USE_TLS=True

# نام کاربری ایمیل
EMAIL_HOST_USER=your-email@gmail.com

# رمز عبور ایمیل
EMAIL_HOST_PASSWORD=your-email-password
```

#### 📊 تنظیمات پایگاه داده
```bash
# URL پایگاه داده (SQLite پیش‌فرض)
DATABASE_URL=sqlite:///db.sqlite3

# برای PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# برای MySQL:
# DATABASE_URL=mysql://user:password@localhost:3306/dbname
```

## 🔧 تنظیمات پیشرفته

### تنظیمات پروتکل‌های VPN
```bash
# پروتکل پیش‌فرض
DEFAULT_PROTOCOL=vless

# پورت‌های پیش‌فرض
DEFAULT_PORTS_VMESS=443
DEFAULT_PORTS_VLESS=443
DEFAULT_PORTS_TROJAN=443

# محدوده پورت‌های قابل استفاده
MIN_PORT=10000
MAX_PORT=65000
```

### تنظیمات زمان انقضا
```bash
# مدت زمان پلن تستی (ساعت)
TRIAL_HOURS=24

# مدت زمان پلن پولی (روز)
PAID_DAYS=30

# مدت زمان تمدید (ساعت)
EXTEND_HOURS=24

# دوره ارفاق (ساعت)
GRACE_PERIOD_HOURS=2
```

### تنظیمات امنیت
```bash
# فعال‌سازی Sniffing
ENABLE_SNIFFING=True

# پروتکل‌های مجاز برای Sniffing
SNIFFING_PROTOCOLS=http,tls,quic

# فعال‌سازی TLS
TLS_ENABLED=True

# فعال‌سازی Reality
REALITY_ENABLED=True

# فعال‌سازی UDP
ENABLE_UDP=True
```

### تنظیمات نام‌گذاری
```bash
# پیشوند inbound های تستی
TRIAL_INBOUND_PREFIX=TrialBot

# پیشوند inbound های پولی
PAID_INBOUND_PREFIX=PaidBot

# پیشوند inbound های کاربر
USER_INBOUND_PREFIX=UserBot
```

### تنظیمات اتصال
```bash
# زمان انتظار اتصال (ثانیه)
CONNECTION_TIMEOUT=15

# تعداد تلاش‌های مجدد
RETRY_ATTEMPTS=5

# تاخیر بین تلاش‌ها (ثانیه)
RETRY_DELAY=2
```

### تنظیمات پیشرفته
```bash
# محدودیت IP برای هر کاربر
LIMIT_IP_PER_USER=3

# تبدیل MB به GB
MB_TO_GB_CONVERSION=1024

# فعال‌سازی لاگینگ
ENABLE_LOGGING=True

# حداکثر اندازه فایل لاگ
MAX_LOG_SIZE=10MB

# تعداد فایل‌های پشتیبان لاگ
LOG_BACKUP_COUNT=5
```

## 📁 ساختار فایل .env کامل

```bash
# ========================================
# تنظیمات اصلی Django
# ========================================

# کلید امنیتی Django - این را تغییر دهید!
SECRET_KEY=your-very-secure-secret-key-here

# حالت توسعه (True برای توسعه، False برای تولید)
DEBUG=True

# میزبان‌های مجاز
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# ========================================
# تنظیمات پایگاه داده
# ========================================

# URL پایگاه داده (SQLite پیش‌فرض)
DATABASE_URL=sqlite:///db.sqlite3

# ========================================
# تنظیمات ربات‌های تلگرام
# ========================================

# توکن ربات ادمین - این را با توکن واقعی جایگزین کنید
ADMIN_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# توکن ربات کاربران - این را با توکن واقعی جایگزین کنید
USER_BOT_TOKEN=1234567890:XYZabcDEFghiJKLmnoPQRstuVWXyz

# رمز عبور ادمین
ADMIN_PASSWORD=your-secure-admin-password

# ID های ادمین تلگرام (با کاما جدا کنید)
ADMIN_USER_IDS=123456789,987654321

# ========================================
# تنظیمات سرور X-UI
# ========================================

# آدرس سرور X-UI
XUI_DEFAULT_HOST=your-xui-server.com

# پورت سرور X-UI
XUI_DEFAULT_PORT=54321

# نام کاربری X-UI
XUI_DEFAULT_USERNAME=admin

# رمز عبور X-UI
XUI_DEFAULT_PASSWORD=your-xui-password

# مسیر پایه وب X-UI (اختیاری)
XUI_WEB_BASE_PATH=/

# ========================================
# تنظیمات ایمیل
# ========================================

# سرور SMTP
EMAIL_HOST=smtp.gmail.com

# پورت SMTP
EMAIL_PORT=587

# استفاده از TLS
EMAIL_USE_TLS=True

# نام کاربری ایمیل
EMAIL_HOST_USER=your-email@gmail.com

# رمز عبور ایمیل
EMAIL_HOST_PASSWORD=your-email-password

# ========================================
# تنظیمات فایل‌های استاتیک و مدیا
# ========================================

# مسیر فایل‌های استاتیک
STATIC_ROOT=staticfiles/

# مسیر فایل‌های مدیا
MEDIA_ROOT=media/

# ========================================
# تنظیمات لاگینگ
# ========================================

# سطح لاگ
LOG_LEVEL=INFO

# مسیر فایل لاگ
LOG_FILE=logs/django.log

# ========================================
# تنظیمات پروتکل‌های VPN
# ========================================

# پروتکل پیش‌فرض
DEFAULT_PROTOCOL=vless

# پورت‌های پیش‌فرض
DEFAULT_PORTS_VMESS=443
DEFAULT_PORTS_VLESS=443
DEFAULT_PORTS_TROJAN=443

# محدوده پورت‌های قابل استفاده
MIN_PORT=10000
MAX_PORT=65000

# ========================================
# تنظیمات زمان انقضا
# ========================================

# مدت زمان پلن تستی (ساعت)
TRIAL_HOURS=24

# مدت زمان پلن پولی (روز)
PAID_DAYS=30

# مدت زمان تمدید (ساعت)
EXTEND_HOURS=24

# دوره ارفاق (ساعت)
GRACE_PERIOD_HOURS=2

# ========================================
# تنظیمات امنیت
# ========================================

# فعال‌سازی Sniffing
ENABLE_SNIFFING=True

# پروتکل‌های مجاز برای Sniffing
SNIFFING_PROTOCOLS=http,tls,quic

# فعال‌سازی TLS
TLS_ENABLED=True

# فعال‌سازی Reality
REALITY_ENABLED=True

# فعال‌سازی UDP
ENABLE_UDP=True

# ========================================
# تنظیمات نام‌گذاری
# ========================================

# پیشوند inbound های تستی
TRIAL_INBOUND_PREFIX=TrialBot

# پیشوند inbound های پولی
PAID_INBOUND_PREFIX=PaidBot

# پیشوند inbound های کاربر
USER_INBOUND_PREFIX=UserBot

# ========================================
# تنظیمات اتصال
# ========================================

# زمان انتظار اتصال (ثانیه)
CONNECTION_TIMEOUT=15

# تعداد تلاش‌های مجدد
RETRY_ATTEMPTS=5

# تاخیر بین تلاش‌ها (ثانیه)
RETRY_DELAY=2

# ========================================
# تنظیمات پیشرفته
# ========================================

# محدودیت IP برای هر کاربر
LIMIT_IP_PER_USER=3

# تبدیل MB به GB
MB_TO_GB_CONVERSION=1024

# فعال‌سازی لاگینگ
ENABLE_LOGGING=True

# حداکثر اندازه فایل لاگ
MAX_LOG_SIZE=10MB

# تعداد فایل‌های پشتیبان لاگ
LOG_BACKUP_COUNT=5
```

## 🔒 نکات امنیتی

### 1. محافظت از فایل .env
```bash
# اضافه کردن .env به .gitignore
echo ".env" >> .gitignore
```

### 2. تغییر کلیدهای امنیتی
- حتماً `SECRET_KEY` را تغییر دهید
- رمزهای عبور قوی انتخاب کنید
- توکن‌های ربات را محافظت کنید

### 3. تنظیمات تولید
```bash
# برای محیط تولید
DEBUG=False
ALLOWED_HOSTS=your-domain.com,www.your-domain.com
```

## 🧪 تست تنظیمات

### 1. بررسی بارگذاری متغیرها
```python
# در Django shell
python manage.py shell

>>> import os
>>> print(os.environ.get('ADMIN_BOT_TOKEN'))
>>> print(os.environ.get('DEBUG'))
```

### 2. تست اتصال به X-UI
```python
# تست اتصال
from xui_servers.services import XUIService

service = XUIService()
if service.test_connection():
    print("اتصال به X-UI موفق")
else:
    print("خطا در اتصال به X-UI")
```

## 🚨 عیب‌یابی

### مشکلات رایج

1. **خطای "ModuleNotFoundError"**
   - مطمئن شوید که `python-dotenv` نصب شده است
   - فایل `.env` در مسیر درست قرار دارد

2. **خطای "Invalid token"**
   - توکن ربات را بررسی کنید
   - مطمئن شوید که ربات فعال است

3. **خطای اتصال به X-UI**
   - آدرس و پورت سرور را بررسی کنید
   - نام کاربری و رمز عبور را چک کنید

4. **خطای "Permission denied"**
   - مجوزهای فایل `.env` را بررسی کنید
   - مسیرهای فایل‌های لاگ را چک کنید

## 📞 پشتیبانی

برای سوالات و مشکلات:
1. فایل لاگ را بررسی کنید
2. تنظیمات را دوباره چک کنید
3. مستندات Django را مطالعه کنید

---

**نکته مهم**: این فایل شامل اطلاعات حساس است. هرگز آن را در مخزن Git قرار ندهید! 
# ========================================
# تنظیمات اصلی Django
# ========================================

# کلید امنیتی Django - این را تغییر دهید!
# برای تولید کلید جدید: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-c^1%va7g4+yqfygvbjku#d4-4d8-sw8rzw9!$_wq-vt(*x-mw9

# حالت توسعه (True برای توسعه، False برای تولید)
DEBUG=True

# میزبان‌های مجاز (برای تولید: your-domain.com,www.your-domain.com)
ALLOWED_HOSTS=localhost,127.0.0.1,*

# تنظیمات زبان و منطقه زمانی
LANGUAGE_CODE=fa-ir
TIME_ZONE=Asia/Tehran
USE_I18N=True
USE_TZ=True

# ========================================
# تنظیمات پایگاه داده
# ========================================

# URL پایگاه داده (SQLite پیش‌فرض)
DATABASE_URL=sqlite:///db.sqlite3

# برای PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# برای MySQL:
# DATABASE_URL=mysql://username:password@localhost:3306/dbname

# تنظیمات پایگاه داده Django
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=

# ========================================
# تنظیمات ربات‌های تلگرام
# ========================================

# توکن ربات ادمین - از @BotFather دریافت کنید
# برای دریافت توکن: /newbot در @BotFather
ADMIN_BOT_TOKEN=8450508816:AAFE6XAj8QvA9iIP12whrKxYRtgsoHFCiFU

# توکن ربات کاربران - از @BotFather دریافت کنید
USER_BOT_TOKEN=8202994859:AAGg68pT5HGR1W9D4pxqnAGeKoZKrD9Dnzs

# رمز عبور ادمین (قوی انتخاب کنید)
ADMIN_PASSWORD=admin123

# ID های ادمین تلگرام (با کاما جدا کنید)
# برای پیدا کردن ID خود: به @userinfobot پیام دهید
ADMIN_USER_IDS=123456789,987654321

# تنظیمات اضافی ربات‌ها
BOT_WEBHOOK_URL=
BOT_WEBHOOK_PATH=/webhook/
BOT_WEBHOOK_SECRET=your_webhook_secret_here

# تنظیمات پیام‌های ربات
WELCOME_MESSAGE=سلام! به ربات VPN خوش آمدید 🚀
HELP_MESSAGE=برای دریافت راهنما از دکمه‌های زیر استفاده کنید 📚
ERROR_MESSAGE=متأسفانه خطایی رخ داد. لطفاً دوباره تلاش کنید ❌

# ========================================
# تنظیمات سرور X-UI سنایی
# ========================================

# آدرس سرور X-UI سنایی (IP یا دامنه)
XUI_DEFAULT_HOST=156.244.31.37

# پورت سرور X-UI سنایی
XUI_DEFAULT_PORT=50987

# نام کاربری X-UI سنایی
XUI_DEFAULT_USERNAME=bUZC0Iovb9

# رمز عبور X-UI سنایی
XUI_DEFAULT_PASSWORD=4jb7doDQZg

# مسیر پایه وب X-UI سنایی
XUI_WEB_BASE_PATH=/YvIhWQ3Pt6cHGXegE4/

# ID اینبوند موجود در سنایی (باید تنظیم شود)
XUI_DEFAULT_INBOUND_ID=1

# تنظیمات امنیتی X-UI سنایی
XUI_USE_SSL=False
XUI_VERIFY_SSL=False
XUI_TIMEOUT=30

# تنظیمات امنیتی X-UI
XUI_USE_SSL=False
XUI_VERIFY_SSL=False
XUI_TIMEOUT=30

# تنظیمات اضافی X-UI
XUI_API_VERSION=v1
XUI_DEFAULT_PROTOCOL=vless
XUI_DEFAULT_PORT_RANGE_START=10000
XUI_DEFAULT_PORT_RANGE_END=65000

# ========================================
# تنظیمات ایمیل
# ========================================

# سرور SMTP
EMAIL_HOST=smtp.gmail.com

# پورت SMTP
EMAIL_PORT=587

# استفاده از TLS
EMAIL_USE_TLS=True

# استفاده از SSL
EMAIL_USE_SSL=False

# نام کاربری ایمیل
EMAIL_HOST_USER=your_email@gmail.com

# رمز عبور ایمیل
EMAIL_HOST_PASSWORD=your_email_password

# آدرس ایمیل فرستنده
DEFAULT_FROM_EMAIL=your_email@gmail.com

# تنظیمات اضافی ایمیل
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_TIMEOUT=30
EMAIL_SSL_KEYFILE=
EMAIL_SSL_CERTFILE=

# ========================================
# تنظیمات فایل‌های استاتیک و مدیا
# ========================================

# مسیر فایل‌های استاتیک
STATIC_ROOT=staticfiles/

# مسیر فایل‌های مدیا
MEDIA_ROOT=media/

# URL فایل‌های استاتیک
STATIC_URL=/static/

# URL فایل‌های مدیا
MEDIA_URL=/media/

# تنظیمات اضافی فایل‌ها
FILE_UPLOAD_MAX_MEMORY_SIZE=2621440
DATA_UPLOAD_MAX_MEMORY_SIZE=2621440
DATA_UPLOAD_MAX_NUMBER_FIELDS=1000

# ========================================
# تنظیمات لاگینگ
# ========================================

# سطح لاگ (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# مسیر فایل لاگ
LOG_FILE=logs/django.log

# تنظیمات اضافی لاگینگ
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_DATE_FORMAT=%Y-%m-%d %H:%M:%S
LOG_MAX_SIZE=10485760
LOG_BACKUP_COUNT=5

# لاگ‌های جداگانه
BOT_LOG_FILE=logs/bot.log
XUI_LOG_FILE=logs/xui.log
ERROR_LOG_FILE=logs/error.log

# ========================================
# تنظیمات پروتکل‌های VPN
# ========================================

# پروتکل پیش‌فرض (vless, vmess, trojan)
DEFAULT_PROTOCOL=vless

# پورت‌های پیش‌فرض
DEFAULT_PORTS_VMESS=443
DEFAULT_PORTS_VLESS=443
DEFAULT_PORTS_TROJAN=443

# محدوده پورت‌های قابل استفاده
MIN_PORT=10000
MAX_PORT=65000

# تنظیمات پروتکل‌های مختلف
PROTOCOL_SETTINGS_VMESS={"network":"ws","security":"none"}
PROTOCOL_SETTINGS_VLESS={"network":"tcp","security":"reality"}
PROTOCOL_SETTINGS_TROJAN={"network":"tcp","security":"tls"}

# تنظیمات Reality (برای VLess)
REALITY_DEST=www.aparat.com:443
REALITY_SERVER_NAMES=www.aparat.com
REALITY_PRIVATE_KEY=YFgo8YQUJmqhu2yXL8rd8D9gDgJ1H1XgfbYqMB6LmoM
REALITY_SHORT_IDS=

# تنظیمات WebSocket (برای VMess)
WS_PATH=/
WS_HOST=

# ========================================
# تنظیمات زمان انقضا
# ========================================

# مدت زمان پلن تستی (ساعت)
TRIAL_HOURS=24

# مدت زمان پلن پولی (روز)
PAID_DAYS=30

# مدت زمان تمدید (ساعت)
EXTEND_HOURS=24

# دوره ارفاق (ساعت)
GRACE_PERIOD_HOURS=2

# تنظیمات اضافی زمان
AUTO_DELETE_EXPIRED_CONFIGS=True
AUTO_DELETE_CHECK_INTERVAL=3600
EXPIRY_WARNING_HOURS=6
EXPIRY_WARNING_MESSAGE=کانفیگ شما تا {hours} ساعت دیگر منقضی می‌شود ⏰

# ========================================
# تنظیمات امنیت
# ========================================

# فعال‌سازی Sniffing
ENABLE_SNIFFING=True

# پروتکل‌های مجاز برای Sniffing
SNIFFING_PROTOCOLS=http,tls,quic

# فعال‌سازی TLS
TLS_ENABLED=True

# فعال‌سازی Reality
REALITY_ENABLED=True

# فعال‌سازی UDP
ENABLE_UDP=True

# تنظیمات امنیتی اضافی
ENABLE_PROXY_PROTOCOL=False
ENABLE_FALLBACK=True
ENABLE_MUX=False
ENABLE_UTLS=False

# تنظیمات فایروال
FIREWALL_ENABLED=False
FIREWALL_RULES=

# ========================================
# تنظیمات نام‌گذاری
# ========================================

# پیشوند inbound های تستی
TRIAL_INBOUND_PREFIX=TrialBot

# پیشوند inbound های پولی
PAID_INBOUND_PREFIX=PaidBot

# پیشوند inbound های کاربر
USER_INBOUND_PREFIX=UserBot

# تنظیمات نام‌گذاری اضافی
INBOUND_SEPARATOR=-
USER_SEPARATOR=_
CONFIG_SEPARATOR=|

# الگوهای نام‌گذاری
TRIAL_CONFIG_PATTERN={prefix}{separator}{user_id}{separator}{protocol}
PAID_CONFIG_PATTERN={prefix}{separator}{user_id}{separator}{plan_id}{separator}{protocol}
USER_CONFIG_PATTERN={prefix}{separator}{user_id}{separator}{protocol}

# ========================================
# تنظیمات اتصال
# ========================================

# زمان انتظار اتصال (ثانیه)
CONNECTION_TIMEOUT=15

# تعداد تلاش‌های مجدد
RETRY_ATTEMPTS=5

# تاخیر بین تلاش‌ها (ثانیه)
RETRY_DELAY=2

# تنظیمات اتصال اضافی
CONNECTION_POOL_SIZE=10
CONNECTION_KEEP_ALIVE=True
CONNECTION_MAX_RETRIES=3
CONNECTION_BACKOFF_FACTOR=2

# ========================================
# تنظیمات پیشرفته
# ========================================

# محدودیت IP برای هر کاربر
LIMIT_IP_PER_USER=3

# تبدیل MB به GB
MB_TO_GB_CONVERSION=1024

# فعال‌سازی لاگینگ
ENABLE_LOGGING=True

# حداکثر اندازه فایل لاگ
MAX_LOG_SIZE=10MB

# تعداد فایل‌های پشتیبان لاگ
LOG_BACKUP_COUNT=5

# تنظیمات عملکرد
CACHE_TIMEOUT=300
SESSION_TIMEOUT=3600
API_RATE_LIMIT=100
API_RATE_LIMIT_PERIOD=60

# تنظیمات پاکسازی خودکار
AUTO_CLEANUP_ENABLED=True
AUTO_CLEANUP_INTERVAL=86400
CLEANUP_LOG_RETENTION_DAYS=30
CLEANUP_TEMP_FILES=True

# تنظیمات مانیتورینگ
MONITORING_ENABLED=True
MONITORING_INTERVAL=300
ALERT_EMAIL_ENABLED=False
ALERT_EMAIL_RECIPIENTS=

# تنظیمات پشتیبان‌گیری
BACKUP_ENABLED=True
BACKUP_INTERVAL=86400
BACKUP_RETENTION_DAYS=7
BACKUP_PATH=backups/

# تنظیمات امنیتی پیشرفته
CSRF_TRUSTED_ORIGINS=
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_FRAME_DENY=True

# ========================================
# تنظیمات پلن‌ها و پرداخت
# ========================================

# تنظیمات پلن‌های پیش‌فرض
DEFAULT_TRIAL_PLAN_NAME=پلن تستی
DEFAULT_TRIAL_PLAN_DURATION=24
DEFAULT_TRIAL_PLAN_TRAFFIC=0
DEFAULT_TRIAL_PLAN_PRICE=0

# تنظیمات پلن‌های پولی
PAID_PLANS_ENABLED=True
PAID_PLANS_DISCOUNT_PERCENT=0
PAID_PLANS_REFERRAL_BONUS=0

# تنظیمات پرداخت
PAYMENT_GATEWAY=
PAYMENT_MERCHANT_ID=
PAYMENT_CALLBACK_URL=
PAYMENT_SUCCESS_URL=
PAYMENT_FAILURE_URL=

# تنظیمات ارز
CURRENCY=IRR
CURRENCY_SYMBOL=تومان
EXCHANGE_RATE=1

# ========================================
# تنظیمات اعلان‌ها و پیام‌ها
# ========================================

# تنظیمات اعلان‌های تلگرام
TELEGRAM_NOTIFICATIONS_ENABLED=True
TELEGRAM_ADMIN_NOTIFICATIONS=True
TELEGRAM_USER_NOTIFICATIONS=True

# تنظیمات پیام‌های سیستم
SYSTEM_MESSAGES_ENABLED=True
WELCOME_MESSAGE_ENABLED=True
HELP_MESSAGE_ENABLED=True
ERROR_MESSAGE_ENABLED=True

# تنظیمات زبان پیام‌ها
MESSAGE_LANGUAGE=fa
MESSAGE_RTL=True
MESSAGE_EMOJI_ENABLED=True 
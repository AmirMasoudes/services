# راهنمای کامل تنظیم X-UI سنایی

## 📋 مراحل تنظیم

### 1. تنظیم فایل env_config.env

ابتدا فایل `env_config.env` را با اطلاعات سرور سنایی خود تنظیم کنید:

```env
# تنظیمات سرور X-UI سنایی
XUI_DEFAULT_HOST=your_sanaei_server_ip
XUI_DEFAULT_PORT=54321
XUI_DEFAULT_USERNAME=admin
XUI_DEFAULT_PASSWORD=your_sanaei_password
XUI_WEB_BASE_PATH=/MsxZ4xuIy5xLfQtsSC/
XUI_DEFAULT_INBOUND_ID=1
```

### 2. ایجاد اینبوند در سنایی

1. به پنل سنایی وارد شوید
2. یک اینبوند جدید ایجاد کنید:
   - **پروتکل:** VLESS
   - **پورت:** 443 یا پورت دلخواه
   - **نام:** `AutoBot` یا نام دلخواه
   - **تنظیمات:** Reality یا TLS

### 3. تست اتصال

اسکریپت تست را اجرا کنید:

```bash
python test_sanaei_connection.py
```

### 4. تنظیم سرور در دیتابیس

از طریق پنل ادمین Django، یک سرور X-UI اضافه کنید:

```python
# در Django Admin یا shell
from xui_servers.models import XUIServer

server = XUIServer.objects.create(
    name="سرور سنایی",
    host="your_sanaei_server_ip",
    port=54321,
    username="admin",
    password="your_password",
    web_base_path="/MsxZ4xuIy5xLfQtsSC/",
    is_active=True
)
```

## 🔧 نحوه عملکرد

### 1. ایجاد کانفیگ تستی

وقتی کاربر درخواست پلن تستی می‌دهد:

1. سیستم به X-UI سنایی متصل می‌شود
2. اینبوند موجود را پیدا می‌کند
3. کلاینت جدید با محدودیت‌های زیر اضافه می‌کند:
   - **زمان:** 24 ساعت
   - **ترافیک:** 1 GB
   - **IP:** نامحدود

### 2. ایجاد کانفیگ پولی

وقتی کاربر پلن پولی خریداری می‌کند:

1. سیستم به X-UI سنایی متصل می‌شود
2. اینبوند موجود را پیدا می‌کند
3. کلاینت جدید با محدودیت‌های پلن اضافه می‌کند:
   - **زمان:** طبق پلن (معمولاً 30 روز)
   - **ترافیک:** طبق پلن
   - **IP:** نامحدود

### 3. تولید کانفیگ

سیستم کانفیگ واقعی از X-UI تولید می‌کند:

```python
# مثال کانفیگ VLESS Reality
vless://uuid@server:port?type=tcp&security=reality&sni=domain&fp=chrome&pbk=key&sid=shortid&spx=%2F#username
```

## 📊 مدیریت خودکار

### 1. پاکسازی خودکار

سیستم به صورت خودکار:

- کلاینت‌های منقضی شده را حذف می‌کند
- ترافیک مصرف شده را بررسی می‌کند
- کانفیگ‌های غیرفعال را پاک می‌کند

### 2. محدودیت‌ها

هر کلاینت دارای محدودیت‌های زیر است:

- **زمان انقضا:** طبق پلن
- **ترافیک:** طبق پلن
- **IP:** قابل تنظیم

## 🚨 عیب‌یابی

### مشکل 1: خطا در اتصال

```bash
# بررسی تنظیمات
python test_sanaei_connection.py
```

**راه‌حل:**

- آدرس IP سرور را بررسی کنید
- پورت 54321 باز است؟
- نام کاربری و رمز عبور صحیح است؟

### مشکل 2: خطا در اضافه کردن کلاینت

**راه‌حل:**

- اینبوند در سنایی وجود دارد؟
- اینبوند فعال است؟
- ظرفیت کلاینت‌ها پر نشده؟

### مشکل 3: کانفیگ کار نمی‌کند

**راه‌حل:**

- پورت در فایروال باز است؟
- تنظیمات Reality صحیح است؟
- دامنه SNI در دسترس است؟

## 📝 نکات مهم

1. **امنیت:** رمز عبور سنایی را قوی انتخاب کنید
2. **پشتیبان‌گیری:** مرتباً از سنایی backup بگیرید
3. **مانیتورینگ:** ترافیک و عملکرد را بررسی کنید
4. **به‌روزرسانی:** سنایی را به‌روز نگه دارید

## 🔄 به‌روزرسانی

برای به‌روزرسانی سیستم:

1. کدهای جدید را دریافت کنید
2. تنظیمات را بررسی کنید
3. تست اتصال را اجرا کنید
4. ربات‌ها را ری‌استارت کنید

## 📞 پشتیبانی

در صورت مشکل:

1. لاگ‌های سیستم را بررسی کنید
2. تست اتصال را اجرا کنید
3. با تیم پشتیبانی تماس بگیرید

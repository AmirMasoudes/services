# تنظیمات نهایی سیستم VPN Bot

## 🔧 تنظیمات X-UI سنایی

- **آدرس سرور**: `156.244.31.37`
- **پورت**: `50987`
- **نام کاربری**: `bUZC0Iovb9`
- **رمز عبور**: `4jb7doDQZg`
- **مسیر وب**: `/YvIhWQ3Pt6cHGXegE4/`
- **آدرس دسترسی**: `https://time.amirprogrammer.ir:50987/YvIhWQ3Pt6cHGXegE4/`
- **SSL**: فعال
- **پنل امن**: بله

## 🤖 تنظیمات ربات‌های تلگرام

### ربات ادمین

- **توکن**: `8450508816:AAFE6XAj8QvA9iIP12whrKxYRtgsoHFCiFU`

### ربات کاربران

- **توکن**: `8202994859:AAGg68pT5HGR1W9D4pxqnAGeKoZKrD9Dnzs`

## ⚠️ موارد باقی‌مانده

تنها موردی که باید تنظیم کنید:

1. **TELEGRAM_ADMIN_ID**: ID تلگرام شما (از @userinfobot دریافت کنید)
2. **XUI_DEFAULT_INBOUND_ID**: شماره inbound موجود در X-UI

## 🚀 مراحل استقرار

### مرحله 1: اجرای اسکریپت کامل استقرار

```bash
# قابل اجرا کردن اسکریپت
chmod +x complete_setup.sh

# اجرای اسکریپت استقرار کامل
./complete_setup.sh
```

### مرحله 2: تنظیم موارد باقی‌مانده

```bash
# ویرایش فایل تنظیمات
nano env_config.env

# تنظیم ID ادمین تلگرام
# TELEGRAM_ADMIN_ID=your_telegram_id_here

# تنظیم شماره inbound
# XUI_DEFAULT_INBOUND_ID=1
```

### مرحله 3: ایجاد کاربر ادمین

```bash
# فعال‌سازی محیط مجازی
source venv/bin/activate

# ایجاد کاربر ادمین Django
python manage.py createsuperuser
```

### مرحله 4: تست سیستم

```bash
# تست اتصال به X-UI
python test_sanaei_connection.py

# بررسی وضعیت سرویس‌ها
supervisorctl status
```

## 📋 دستورات مفید

### مدیریت سرویس‌ها

```bash
# بررسی وضعیت
supervisorctl status

# راه‌اندازی مجدد
supervisorctl restart django
supervisorctl restart telegram_bot

# مشاهده لاگ‌ها
tail -f /var/log/django.log
tail -f /var/log/telegram_bot.log
```

### مدیریت Django

```bash
# فعال‌سازی محیط مجازی
source venv/bin/activate

# اجرای سرور توسعه
python manage.py runserver 0.0.0.0:8000

# اجرای مایگریشن‌ها
python manage.py migrate

# دسترسی به Django shell
python manage.py shell
```

## 🔍 آدرس‌های مهم

- **وب‌سایت**: `http://your_server_ip`
- **پنل ادمین**: `http://your_server_ip/admin`
- **X-UI سنایی**: `https://time.amirprogrammer.ir:50987/YvIhWQ3Pt6cHGXegE4/`

## 🛠️ عیب‌یابی

### مشکل اتصال به X-UI

```bash
# تست اتصال ساده
python test_connection_simple.py

# بررسی پورت
netstat -tlnp | grep 50987

# تست با curl
curl -k -v https://time.amirprogrammer.ir:50987/YvIhWQ3Pt6cHGXegE4/login
```

### مشکل Telegram Bot

```bash
# بررسی لاگ‌های Bot
tail -f /var/log/telegram_bot.log

# تست دستی Bot
source venv/bin/activate
python bot/user_bot.py
```

### مشکل Django

```bash
# بررسی لاگ‌های Django
tail -f /var/log/django.log

# بررسی تنظیمات
python manage.py check --deploy
```

## 📊 مانیتورینگ

```bash
# بررسی استفاده از منابع
htop

# بررسی فضای دیسک
df -h

# بررسی اتصالات شبکه
netstat -tlnp
```

## 🔒 امنیت

```bash
# تغییر مجوزهای فایل‌ها
chmod 600 env_config.env
chmod 644 db.sqlite3

# بررسی فایروال
ufw status
```

## 📝 نکات مهم

1. **امنیت**: حتماً رمزهای عبور قوی استفاده کنید
2. **پشتیبان‌گیری**: از دیتابیس و فایل‌های مهم پشتیبان تهیه کنید
3. **به‌روزرسانی**: مرتباً سیستم را به‌روزرسانی کنید
4. **مانیتورینگ**: لاگ‌ها را مرتباً بررسی کنید

## 🆘 پشتیبانی

در صورت بروز مشکل:

1. لاگ‌های مربوطه را بررسی کنید
2. تنظیمات را دوباره بررسی کنید
3. از صحت اتصال به X-UI اطمینان حاصل کنید
4. در صورت نیاز، سیستم را راه‌اندازی مجدد کنید

---

**📚 مستندات کامل**: `DEPLOYMENT_GUIDE.md`
**🔧 راهنمای X-UI**: `Sanaei_XUI_Setup_Guide.md`

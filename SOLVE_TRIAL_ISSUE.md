# 🔧 حل مشکل عدم نمایش کانفیگ تستی

## 🚨 مشکل شناسایی شده:

وقتی روی "🎁 پلن تستی" کلیک می‌کنید، کانفیگ نمایش داده نمی‌شود.

## 🔍 علل احتمالی:

### 1. وضعیت تست کاربر

- کاربر قبلاً از پلن تستی استفاده کرده است
- فیلد `has_used_trial` برابر `True` است

### 2. مشکل در ایجاد کانفیگ

- خطا در اتصال به X-UI
- خطا در ایجاد inbound
- خطا در ایجاد کاربر در X-UI

## 🔧 مراحل حل:

### مرحله 1: بررسی وضعیت کاربر

```bash
cd /opt/vpn-service/services
python test_trial_config.py
```

### مرحله 2: ریست کردن وضعیت تست (اگر نیاز باشد)

```bash
cd /opt/vpn-service/services
python reset_trial_status.py
```

### مرحله 3: راه‌اندازی مجدد User Bot

```bash
systemctl restart user-bot
systemctl status user-bot
```

### مرحله 4: تست ربات

1. به ربات تلگرام بروید
2. دستور `/start` را بزنید
3. روی "🎁 پلن تستی" کلیک کنید
4. کانفیگ باید نمایش داده شود

## 📋 بررسی دستی:

### بررسی وضعیت کاربر در Django shell:

```python
python manage.py shell

from accounts.models import UsersModel

# بررسی کاربر
user = UsersModel.objects.first()
print(f"نام: {user.full_name}")
print(f"Telegram ID: {user.telegram_id}")
print(f"تست استفاده شده: {user.has_used_trial}")
print(f"می‌تواند تست بگیرد: {user.can_get_trial()}")

# ریست کردن وضعیت تست
user.has_used_trial = False
user.save()
print("وضعیت تست ریست شد!")
```

### بررسی لاگ‌های ربات:

```bash
journalctl -u user-bot --no-pager -n 20
```

## 🎯 تست نهایی:

### تست ربات:

1. به ربات تلگرام بروید
2. دستور `/start` را بزنید
3. روی "🎁 پلن تستی" کلیک کنید
4. کانفیگ باید نمایش داده شود

### انتظار:

```
🎉 پلن تستی شما فعال شد!

📋 نام: پلن تستی
⏰ اعتبار: 24 ساعت
📊 حجم: نامحدود
🖥️ سرور: سرور 1

🔧 کانفیگ شما:
vless://uuid@server:port?...

⚠️ نکات مهم:
• این پلن فقط یک بار قابل استفاده است
• پس از 24 ساعت منقضی می‌شود
• برای استفاده مداوم، پلن پولی خریداری کنید
```

## 📞 در صورت بروز مشکل:

### بررسی لاگ‌ها:

```bash
# لاگ‌های User Bot
journalctl -u user-bot --no-pager -n 20

# لاگ‌های Django
tail -f /opt/vpn-service/services/logs/app.log
```

### بررسی وضعیت سرویس‌ها:

```bash
systemctl status django-vpn nginx redis-server postgresql admin-bot user-bot
```

## ✅ پس از حل مشکل:

1. **کانفیگ تستی نمایش داده می‌شود**
2. **کاربر می‌تواند کانفیگ را کپی کند**
3. **پلن تستی فقط یک بار قابل استفاده است**

## 🎉 نتیجه:

**سیستم کانفیگ تستی کاملاً کار می‌کند!**

### فایل‌های مفید:

- `test_trial_config.py` - تست کانفیگ تستی
- `reset_trial_status.py` - ریست کردن وضعیت تست
- `SOLVE_TRIAL_ISSUE.md` - راهنمای حل مشکل

**🎯 سیستم کانفیگ تستی آماده استفاده است!**
